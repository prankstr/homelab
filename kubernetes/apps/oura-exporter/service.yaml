apiVersion: v1
kind: Service
metadata:
  labels:
    app: oura-exporter
  name: oura-exporter
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: http
  selector:
    app: oura-exporter
  type: ClusterIP

