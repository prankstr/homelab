apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kasten
spec:
  interval: 10m
  chart:
    spec:
      chart: k10
      version: 8.0.11
      sourceRef:
        kind: HelmRepository
        name: kasten
        namespace: kasten
      interval: 1h
  values:
    operator:
      create: true
